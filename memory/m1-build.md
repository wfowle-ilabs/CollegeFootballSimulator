---
name: m1-build
description: Code layout and build/test commands for the College Football Simulator (M1 + M2 complete)
metadata:
  type: project
---

College Football Simulator code is a .NET solution **`CollegeFootballSimGoDot.sln`** (classic format — Godot 4.6 generates/manages a `.sln`, NOT dotnet 10's default `.slnx`; created with `dotnet new sln --format sln`). Four projects:
- `src/CfbSim.Core/CfbSim.Core.csproj` — engine-free .NET class library (net8.0), **zero Godot dependency**. Holds the sim:
  - M1: `Rng` (`Pcg32Rng`/`IRng`), `Dice` (`DiceRoller`, `RollMode`, `D20Roll`), `Ratings` (`RatingMath`: blend + modifier), `Checks` (`CheckResolver`, `CheckResult`), `Model` (`Player`, `Team`, `Coach`, enums — **plain C# POCOs**, Resource/serialization deferred to M5), `Generation` (`PlayerGenerator`, `PositionProfiles`, `RosterSpec`, `NamePool`), `Sim.InsideRunResolver`.
  - M2: `Sim.Play` (`PlayOutcome`, `RunPlayResolver`, `PassPlayResolver`, `SpecialTeamsResolver`, enums), `Sim.PlayCalling.PlayCaller` (`PlaySituation`), `Sim.Game` (`GameSimulator`, `DriveSimulator`, `GameClock`, `Field`, `DriveResult`), `Stats` (`BoxScore`, `StatLines`, `StatAggregator`), `Events` (`EventBus`, `DomainEvent`, `GameConcluded`, `ScoreChanged`, `IEventSink`), `Model.GameResult`. Entry point: `GameSimulator.Simulate(rng, home, away, sink?, passBias?)`.
- `tests/CfbSim.Tests/CfbSim.Tests.csproj` — xUnit, headless. Run: `dotnet test tests/CfbSim.Tests/CfbSim.Tests.csproj`. (29 tests green at M2.)
- `samples/CfbSim.Demo/CfbSim.Demo.csproj` — console runner: simulates a full game + box score. **Arg passing:** `dotnet run --project samples/CfbSim.Demo -- <seed>` (the `--` is REQUIRED; run options otherwise swallow the arg) or run the built DLL directly: `dotnet samples/CfbSim.Demo/bin/Debug/net8.0/CfbSim.Demo.dll <seed>`.
- `CollegeFootballSimGoDot.csproj` (repo root) — Godot game project, `Godot.NET.Sdk/4.6.3`, net8.0. Uses `EnableDefaultCompileItems=false` + `Compile Include="src/Game/**/*.cs"` so it only compiles Godot-side code and references the core lib. Godot-side smoke test: `src/Game/SmokeTest.cs` (simulates a full game, prints to Output). Main scene `Main.tscn` (root Node + SmokeTest script) is set via `run/main_scene` in `project.godot`, so pressing Play in the Godot editor runs it.

Godot editor notes: open the project in the .NET build of Godot 4.6.3, let it build (Build button / on first run), press Play to see the smoke test in the Output panel. Core/sim development (M1-M6) needs no editor — it's all in `CfbSim.Core`, built/tested with `dotnet`. Editor matters at M7 (UI).

Naming gotcha avoided: static classes are `DiceRoller` and `RatingMath` (NOT `Dice`/`Ratings`) to avoid clashing with their own namespaces.

Determinism: everything random flows through `IRng` (seeded PCG32). Same seed + inputs ⇒ identical output. Don't use `System.Random` or Godot RNG in the core.

**M1 complete:** data model + `PlayerGenerator` + inside-run `PlayResolver` (push → second level → open field). Mechanics spec: `docs/mechanics.qmd`.

**M2 complete:** full play-by-play game engine — `GameSimulator` runs drives via `DriveSimulator` with clock/downs/field position/scoring/turnovers/punts/FGs/PAT/OT, an AI `PlayCaller` (offense play choice + defensive key), `StatAggregator`→`BoxScore`, and `GameConcluded` emitted on the `EventBus`. Playbook: inside/outside run, short/deep pass, punt, FG, XP. Kickoffs simplified to touchback-at-25; kickoff returns, 2-pt, timeouts/2-min logic deferred. Numeric constants (clock seconds, INT rates, yard curves) are first-pass — **full balance tuning is M9**. Produces believable finals (~19-11, 42-2) and box scores; ~160 plays/game.

**M3 complete:** league + season.
- `Model`: `League`, `Conference`, `Matchup`/`Schedule`, `SeriesRecord`. `Data`: `FbsLeagueData` (real ~135 FBS teams/conferences + prestige — alignment is ~2025, editable; Pac-12 is the 2-team post-collapse version), `RivalryData` (named protected rivalries). `Generation.LeagueBuilder`.
- `Services.SeriesHistory`: sparse head-to-head records (created on first meeting), named rivalries pre-seeded via `SeededFor(league)`; supports promoting a series to a rivalry.
- `Sim.Season`: `ScheduleBuilder` (per-conference round-robin via circle method + protected rivalries forced in + cross-conf non-conf fill + greedy week assignment, ~12 games/team over 14 weeks), `ConferenceScheduleRule`/`ScheduleRules` (divisions modeled but unused — hook for later), `SeasonSimulator` (week loop → `GameSimulator`, updates records + history, emits `WeekAdvanced`/`SeasonConcluded`), `StandingsService` + `TiebreakService`, `RankingService` (top-25, record + SOS-by-prestige + prestige prior — simplistic, improve later), `SeasonResult`/`TeamRecord`/`SeasonGameResult`/`RankedTeam`.
- Demo: `dotnet run --project samples/CfbSim.Demo -- league` (alignment), `-- season [seed]` (full season: top 25, P4 leaders, rivalry scores), `-- [seed]` (one game UGA vs ALA). 45 tests green.

**M4 complete:** postseason. `Sim.Postseason`: `ConferenceChampionships` (top-2 of each conference's standings play a title game; updates records + history), `CfpSelector` (12-team field: 5 highest-ranked conf champions auto-bid + 7 at-large, straight seeding 1–12), `BracketSimulator` (byes for seeds 1–4; First Round 8v9/5v12/7v10/6v11; Quarterfinal/Semifinal/National Championship; 11 games; higher seed nominal home, neutral, no HFA modeled), `BowlSelector` (light non-CFP bowl slate, 6+ win teams), `PostseasonSimulator` orchestrator, result types in `PostseasonResults.cs`. New event `NationalChampionCrowned`. Demo `-- season [seed]` runs season **and** postseason (conf champs, CFP field/bracket, champion, final Top 10). 51 tests green.

**Rule (per user):** EVERY game between two teams updates both head-to-head history AND the teams' season W/L records — regular season, conference championships, CFP, and bowls (the only future exception is the intra-squad spring game). Postseason carries real weight: `PostseasonSimulator` produces two polls — `SelectionRankings` (after champ games, seeds the CFP) and `FinalRankings` (after the whole postseason, with a playoff/bowl **achievement bonus** in `RankingService.Rank(..., achievementBonus)` so the champion finishes #1 and deep runs move the poll). Postseason W/L is applied centrally via `ApplyToRecords` (Wins/Losses/points; not ConfWins). Example: champion finishes 15-1 and #1.

The v1 Definition-of-Done core loop (start → season → champion) is now achievable headless.

**M5 complete:** Save/Load & Storage — **engine-free JSON** (System.Text.Json), NOT Godot ResourceSaver/.tres (keeps core testable headless; Godot will pass a globalized `user://` dir path later).
- `Save/SaveManager`: thin **spine** (`spine.json`: schemaVersion, year, userTeamId, RNG snapshot, sidecar manifest) + per-module **sidecars** (`league.json` current-state, `season.json` season-state, `history.json` head-to-head). Atomic writes (temp + `File.Move` overwrite). Schema version check throws `SaveManager.SaveVersionException`. `JsonSerializerOptions` uses `JsonStringEnumConverter` + **`PreferredObjectCreationHandling = Populate`** (REQUIRED — without it STJ skips our get-only collections like `Conferences`/`Teams`/`Roster`/`Schedule.Games` and they load empty). `Save/GameSave` is the in-memory aggregate.
- Resumable season: `SeasonState` (Schedule + Records + Games + NextWeek, persistable) + `SeasonDriver` (`Initialize`/`AdvanceWeek`/`ToResult`); `SeasonSimulator.Run` is now a thin wrapper over it (same behavior/determinism, M3/M4 unaffected).
- RNG resume: `Pcg32Rng.Snapshot()` → `Pcg32State` (state, inc, gaussian spare) and `Pcg32Rng.Restore(state)`.
- `SeriesHistory.Snapshot()`/`FromRecords()`. Computed get-only props across the model carry `[JsonIgnore]` (esp. `League.AllTeams`).
- Determinism on resume verified: save mid-season → reload → continue produces identical final records to an uninterrupted run. Demo `-- save`. 56 tests green. league.json ≈1.4MB (binary/compression is a later optimization). Per-sidecar (vs spine-level) version stamping deferred.

**M6 complete:** offseason & multi-season. `Sim.Offseason`: `OffseasonStage.Run` (archive completed season → `LeagueHistory`; advance class years; graduate seniors; refill each roster to `RosterSpec` with `PlayerGenerator.GenerateFreshman` walk-ons at team prestige) and `SeasonCycle` (`NewGame` builds league + primes id allocator; `RunFullYear` = schedule → season → postseason → offseason, advances `save.Year`). `Player.Class` is now settable (players age in place). `Model.LeagueHistory`/`SeasonSummary`/`SeasonFinish` = the season-by-season archive (champion, conf champions, all-team finishes). `GameSave` gained `Archive` (LeagueHistory) + `NextPlayerId`; **save schema bumped to v2** with new `archive.json` sidecar; spine carries `NextPlayerId`. Demo `-- multi [years]`. 61 tests green.

**v1 offseason design (resolves open Q#10):** NO attribute development in v1 — just age + graduate + walk-on replenish (deep progression is v2). Walk-ons generated at full team prestige (no decay) so the league stays stable until real recruiting (v2) adds talent variance.

**Known balance note (M9/v2):** with no recruiting/development variance, the highest-prestige team repeats as champion a lot (e.g., Georgia won 5 of 6 in one run). Parity comes later via recruiting (v2) and balance tuning (M9).

**M7 complete:** playable Godot UI shell. **Code-driven UI** (programmatic Godot Control nodes, no per-screen .tscn) so it builds/verifies via `dotnet build` (can't run the editor headless here; user verifies visually). `Main.tscn` root is now a `Control` with `src/Game/App.cs` (replaced SmokeTest.cs, which was deleted). Game-side code in `src/Game/`:
- `App.cs` — composition root + screen manager (swaps screens).
- `GameManager.cs` — flow/state controller (the SimDriver role): NewGame/SelectTeam/SimWeek/SimToEndOfSeason/RunPostseason/StartNextSeason/SaveGame/Continue + view helpers (CurrentRankings, UserConferenceStandings, UserResults). Save path = `ProjectSettings.GlobalizePath("user://saves/slot1")`.
- `UI/`: `Ui.cs` (control factory helpers), `MainMenuScreen` (New Game/Continue/Quit), `TeamSelectScreen` (conference→team), `SeasonHubScreen` (header record+week, Sim Week / Sim to End / Save / Main Menu, live conference standings + Top 25 + your results; "Go to Playoff" when season complete), `PostseasonScreen` (champion banner, CFP bracket, final Top 25, Start Next Season).
- Flow: Main Menu → Team Select → Season Hub (loop weeks) → Postseason → next season → Hub.
- **Godot game csproj now has `<ImplicitUsings>enable</ImplicitUsings>`** (it didn't before; UI code needs System/Linq/Generic). Screens are plain builder classes (not Node scripts) returning a Control tree — instantiated via `new`, no scene registration needed. No autoloads (folded into App/GameManager) to avoid project.godot churn; can promote later. Sim is synchronous (no background threading yet). 61 tests still green; whole solution builds.

To run: open in Godot editor (.NET build), Build, press Play.

**M8 complete:** media & narratives (event-driven). `CfbSim.Core/Media/`: `NewsArticle` (+ `ArticleType`), `NarrativeContext`, `INarrativeGenerator` (seam for future `LlmNarrativeGenerator` — LLamaSharp/GGUF, not built), `TemplateNarrativeGenerator` (deterministic templated text, phrasing varied by a stable hash — does NOT touch sim RNG, so determinism/resume preserved), `MediaStore` (per-save cache: ForWeek/Featured/ForTeam/WeeksWithCoverage), `MediaSubsystem` (builds `NarrativeContext` per game, **tiers** coverage → full article for user/rivalry/upset/both-ranked/top-10-winner, short recap otherwise; upset = prestige gap ≥12 or ranked-loses-to-unranked). Event-driven: `MediaSubsystem` consumes via `EventBus` — `GameManager` subscribes to `WeekAdvanced` and generates that week's coverage; `SeasonDriver.AdvanceWeek` is called with `_bus` from the game flow (headless season/multi tests pass no sink → no media, fine). Persisted: `GameSave.Media` + `media.json` sidecar; **save schema bumped to v3**. UI: `src/Game/UI/MediaScreen.cs` (week selector + team filter + headline list + reading pane), reached via a "Media" button on the Season Hub. Demo `-- media`. 66 tests green. Generation is synchronous (architecture wants off-thread between weeks — deferred); media kept for all weeks (retention/pruning deferred).

**UI restructure (post-M8, user-requested):** the in-game UI is now a **top-nav shell** (`src/Game/UI/GameShell.cs`) with persistent header + sim controls (Sim Week / Sim to End / Go to Playoff / Save / Main Menu) and sections that swap content: **Season** (`SeasonView`: schedule, conference standings, Top 25), **Team** (`TeamView`: Roster tab + Depth Chart tab — pick position, "Set as Starter" via `GameManager.PromoteToStarter`, which reorders the roster so `Team.Starter(pos)` returns him), **Stats** (`StatsView`: season passing/rushing/receiving leaders), **History** (`HistoryView`: champions by year, all-time titles, your program's finishes from `LeagueHistory`), **Media** (`MediaView`, refactored from the old MediaScreen). Old `SeasonHubScreen`/`MediaScreen` deleted. `App.ShowGame()` hosts the shell.

**New core feature for the Stats view:** season-long player stats now accumulate. `Sim/Season/StatBook` (per-player `PlayerStatLine` totals + `Leaders(by, n, teamId?)`) lives on `SeasonState.Stats`; `SeasonDriver.AdvanceWeek` folds each game's `BoxScore` into it. Serialized in season.json (no schema bump — additive). 68 tests green. Note: only regular-season games accumulate stats (postseason stats deferred); depth-chart management is just starter-promotion for now.

**UX pass 2 (user-requested):** color + card "widget" styling (`Ui.cs` palette: dark stadium theme, `Card`/`ScrollText`/`ScrollBox`/`RowButton` helpers, styled buttons; `App` adds a background `ColorRect`; `GameShell` highlights the active section). **Step-wise playoff** (replaced instant-sim): core `PostseasonDriver` + `BracketState`/`BracketGame`/`PostseasonState` — `Begin` (conf champs → re-rank → seed → empty bracket) then `AdvanceRound` (sims one round, propagates winners, finalizes bowls+final poll when done). `PostseasonSimulator.Run` now wraps the driver (headless unchanged; 68 tests still green, determinism preserved). Old `BracketSimulator` deleted. `PostseasonScreen` shows the seeded bracket and a "Sim Next Round" button; champion crowned on the final. **Clickable games → box scores:** `SeasonDriver.AdvanceWeek` + `PostseasonDriver` take optional box sinks; `GameManager` caches per-game `BoxScore`s in a session dict (`_boxes`, cleared each StartSeason; NOT persisted — past boxes gone after reload) with `SeasonBox`/`PlayoffBox` lookups; `BoxScorePopup` (AcceptDialog) renders a box; Season schedule rows + bracket games are clickable. GameManager postseason API: `EnterPostseason`/`AdvancePlayoffRound`/`Bracket`/`PlayoffComplete`.

**UX pass 3 (navigation + detail views):** introduced a **navigation framework** in `src/Game/UI/`: `Nav.cs` = `Page` (abstract, keeps filter/state in fields), `INav`, `NavHost` (breadcrumb + page stack; `SetRoot`/`Push`/`PopTo`/`Refresh`; pushing a detail page keeps parents alive so state survives back-navigation). Both `GameShell` (in-season) and `PostseasonScreen` embed a `NavHost`. Section content + the bracket are now `Page`s under `src/Game/UI/Pages/`: `SeasonPage`, `StatsPage`, `HistoryPage`, `MediaPage`, `TeamProfilePage` (overview + clickable schedule + roster/conference links; Team section root = user's TeamProfilePage), `RosterPage` (grouped by position, **filter by position + class**, clickable players), `PlayerProfilePage` (bio/attributes/skills/season stats — bio fields we don't model show "—"), `ConferencePage`, `DepthChartPage` (starter promotion), `BracketPage`, `BoxScorePage` (comprehensive ESPN-style box: team comparison grid + per-team passing/rushing/receiving/defense; unrecorded stats like 3rd-down/penalties/red-zone are "—" placeholders). Teams/players/games are clickable through to detail everywhere; breadcrumb navigates back preserving filter state. Old `*View.cs` + `BoxScorePopup.cs` deleted (box score is now a pushed page, not a dialog). New `GameManager` accessors: `RecordOf/RankOf/ScheduleOf/ResultFor/HomeAwayRecord/NextOpponent/PlayerStat/PlayerById/ConferenceOf/ConferenceById/ConferenceStandings`. `Ui.LinkButton` added. 68 tests still green (UI is game-side only). Compile-verified, not eye-verified.

**UX pass 4 (Season hub + calendar/broadcast):** schedule now carries a real calendar. Core `Sim/Season/Broadcast.cs`: `TimeSlot` enum (Noon/Afternoon/Primetime), `SeasonCalendar` (week 1 = last Saturday of August; `SaturdayOfWeek`/`Kickoff`/date+time labels), `BroadcastScheduler.Assign` (deterministic off seeded RNG + game "appeal" = combined prestige + rivalry bump → marquee game Sat primetime on a flagship, a couple flex to Thu/Fri night, conference-mapped networks SEC/B1G/ACC/B12/G5). `Matchup` gained `Kickoff` (DateTime) + `Network` (string) + derived `Slot` (additive — no save schema bump; broadcast data is presentation-only, never feeds sim, determinism preserved; 68 tests still green though the RNG-stream shift changes absolute scores vs pre-pass — resume-vs-uninterrupted determinism still holds). `ScheduleBuilder.Build` calls `BroadcastScheduler.Assign` at the end. `Matchup` now references `CfbSim.Core.Sim.Season` for `TimeSlot`. New `GameManager` accessors: `CurrentWeek`/`TotalWeeks`/`WeekGames`/`UserGameInWeek`/`UserNextGame`/`GameOfTheWeek`/`LatestHeadlines`/`SeriesVs`/`StreakOf`/`UserStreak`. **Season UI is now two sub-tabs** (`SeasonPage` rewritten): **Overview** = Next-Game spotlight (opp+rank, date/time/network, series record, streak; BYE/"season complete" fallback) + national Game-of-the-Week card + Headlines card (latest 3 articles → MediaPage) + horizontal **daily strip** (Thu/Fri/Sat day tiles, each with 3 slot rows, user's game shown in its slot — only the user's games, per request) + the 3 existing lists (schedule shows date/network for upcoming; standings appends streak; Top 25 appends conf abbr). **Calendar** sub-tab = filter bar (My Team default / My Conference / Top 25 / Rivalries / All) over the whole season's slate, weeks as headers, clickable rows. New `Ui.Chip` (rounded badge) + `Ui.Tile` (titleless card) helpers. **GameShell** relocated: focal week/date indicator ("Week 6 of 14 · week of Sat, Oct 10") with Sim Week/Sim to End right beside it; Save/Main Menu tucked top-right. **Sim Week confirmation**: `ConfirmationDialog` with "Don't ask again" checkbox; opt-out persisted via new `src/Game/AppSettings.cs` (Godot `ConfigFile` at `user://settings.cfg`). Compile-verified (`dotnet build` clean), not eye-verified.

**UX pass 5 (day cursor + month calendar + School tab):** the season now advances **day by day**, not just by week. Core: `SeasonState.CurrentDate` (DateOnly day cursor, additive — save schema unchanged, round-trips via STJ DateOnly); `SeasonCalendar` gained `OpeningSunday`/`SundayOfWeek`/`WeekOf`; `SeasonDriver` refactored to share a private `SimMatchup` between `AdvanceWeek` and the new **`AdvanceDay`** (sims only games whose `Kickoff.Date == next`; rolls `NextWeek` forward + fires `WeekAdvanced` per completed week via a `WeekComplete` check, so media/standings still update on week boundaries; both paths skip already-`Played` games). Day-stepping and week-stepping produce DIFFERENT seasons (RNG draw order differs by sim order) — that's fine, they're separate runs; headless tests/`SeasonSimulator` still use `AdvanceWeek` (unchanged, 68 prior tests green). New `tests/SeasonDayDriverTests.cs` (2 tests): day-stepping completes the season, plays every game once, fires one WeekAdvanced/week, deterministic. **70 tests green.** GameManager: `CurrentDate`/`CurrentWeek` (from cursor via `WeekOf`)/`AdvanceDay`/`SimToEndOfWeek` (stops at next Saturday)/`SimToMyNextGame`/`SimToEndOfSeason` all day-step via private `Day()`; `SimWeek` is now an alias for `SimToEndOfWeek`; added `GamesOn(date)`/`RivalriesOf`/`ConferenceTitlesFor`. **UI:** Season Overview's daily strip now shows **all 7 days** (Sun–Sat) with the **current day highlighted** (accent border + TODAY chip via new `Ui.Tile(highlight)`); Calendar sub-tab is a **month-grid** (7-col `GridContainer` per month, leading blanks, day cells list filtered games, today highlighted) replacing the old week-list. **GameShell** controls reworked: focal label is now the **current date** ("Sunday, August 30, 2026") with a Week/record subline; **Advance Day** button + **"Sim to ▾" `MenuButton`** (My Next Game / End of Week / End of Regular Season); confirmation dialog (with "Don't ask again" → `AppSettings.ConfirmSimWeek`) now gates the multi-day **Sim-to** checkpoints (not single-day Advance); each advance plays a short fade-in **transition** (`Tween` on the NavHost content modulate). New **School** top-nav tab between Stats and History → `SchoolPage` (identity: program/conference/prestige bar; this-season snapshot; program honors from `LeagueHistory`; rivalries) — the program-at-a-glance view distinct from the roster-focused Team tab. New `Ui.MenuButton`/`Ui.Tile(highlight)`/`Ui.Chip` helpers. Compile-verified, not eye-verified.

**UX pass 6 (slots/times, month calendar, right-click sim, training, slide-over menu):** `TimeSlot` enum **renamed** `Noon/Afternoon/Primetime` → **`Morning/Afternoon/Evening`** (12:00/3:30/7:30 PM ET fixed times); `SeasonCalendar.SlotTime`/`SlotTimeLabel` added; definitive ET kickoff times now shown in spotlight/strip/slots (label is " ET", covers EST/EDT). **Calendar** is now **one month at a time** with ◀ Prev / Next ▶ arrows (clamped to season range; `_calMonth` page state) — the old useless filter bar is **deleted**; cells show user games + any ranked matchup (fixed `CalendarShows` rule). **Right-click "sim to here"**: new `INav.RequestSim(what, perform)` (NavHost takes optional `Action<string,Action> onSim`; GameShell wires it to `ConfirmSim`+`AfterSim`); SeasonPage attaches a right-click `GuiInput` handler on calendar day cells AND week day tiles → `GameManager.SimToDate(date)` (only for future dates). **Training slots (2K-style, scaffolded):** `Sim/Season/Training.cs` — `TrainingActivity` enum + `TrainingCatalog` (Rest/PositionDrills/Conditioning/FilmStudy/Scrimmage/SpecialTeams, each with Boost + Tradeoff text) + `TrainingKey.Of(date,slot)`; `SeasonState.Training` (`Dictionary<string,TrainingActivity>`, additive, persists). Week-strip non-game slots render an assignable `MenuButton` (today-or-future only; tooltip shows boost/tradeoff) → `GameManager.TrainingFor`/`SetTraining`. **Effects are temporary next-game prep by design (no permanent dev — stays v1-consistent); the numeric SIM WIRING is NOT done yet — assignments are catalogued + persisted only, flagged as the next pass.** Model from 2K (temporary boosts vs energy) + CFB25 (position drills, fatigue/injury). **Hamburger slide-over menu:** GameShell top-right Save/Main Menu replaced by a `☰ Menu` button opening a right-anchored `PanelContainer` that slides in (Tween on offset_left/right, dimmer click-catcher; `Ui.SidePanelStyle`); contains Save / Main Menu / Close. **Unsaved-changes guard:** `GameManager.HasUnsavedChanges` (`_dirty` set on any sim/training/new-game/offseason, cleared on Save/Continue); leaving to Main Menu with unsaved progress pops a dialog (Save & Exit / Exit Without Saving / Cancel). **`GameShell.Build` now returns a full-rect `Control`** hosting padded content + the overlay (overlay must sit outside the layout VBox to not resize content). **Team view gained a "View School" button** → `SchoolPage(teamId)`. New `Ui.SidePanelStyle`; `Ui.MenuButton`/`Tile(highlight)`/`Chip` reused. 70 tests green; compile-verified, not eye-verified. ☰ glyph may be tofu in the default font — swap to an icon if so.

**M-feature pass (training boosts in-sim + special teams):** training slots now have REAL sim effects. `Model/TeamBoost.cs` — transient per-game prep modifier (Offense/Defense/SpecialTeams bumps + Fatigue tradeoff; `OffenseBonus = Offense - Fatigue` etc.); `Team.ActiveBoost` ([JsonIgnore], default `TeamBoost.None` — never mutate the singleton, always assign fresh). `Sim/Season/TrainingBoosts.cs` — `ForGame(state, teamId, week, userTeamId?)`: user team → boost from saved plan (gathers the week's Sun–Sat training entries); every other team → **deterministic AI plan** from a throwaway `Pcg32Rng` seeded by (teamId,week) so it never touches the sim's seeded stream. Activity→(off,def,st,fat) map: Rest(0,0,0,-3) PositionDrills(2,2,0,2) Conditioning(0,0,0,-2) FilmStudy(2,2,0,0) Scrimmage(3,3,0,4) SpecialTeams(0,0,4,1); fatigue clamped ≥0, bonuses ≤6 (overtraining nets negative). Boosts applied in resolvers by ADDING `off`/`def` to the skill inputs of `ResolveContest` (Run: push/2nd/ballsec/openField — OpenField gained off/def params; Pass: prot/throw/yac; SpecialTeams: KickPower/KickAccuracy + `SpecialBonus`). `SeasonDriver.SimMatchup` sets home/away `ActiveBoost` before `GameSimulator.Simulate`, clears to `None` after; `AdvanceWeek`/`AdvanceDay` gained optional `userTeamId` (GameManager.Day passes `Save.UserTeamId`; headless/`SeasonSimulator` pass null → all-AI, still deterministic). `GameManager.BoostFor(teamId)` (next-game week) → shown on `SchoolPage` (Training Boost card) + `PlayerProfilePage` (Training card, position-relevant phase). **Special teams added to the sim:** two-point conversions (`DriveSimulator.GoForTwo` = late-game 2-pt chart {-2,-5,-10,-16,1,4,5,11,12}; `TwoPointTry` = one goal-line snap via Run/Pass resolver, good if yards≥3 → 8 pts); **kickoff returns** (`SpecialTeamsResolver.KickoffReturnSpot` ~65% touchback-to-25 else returner Elusiveness/Speed → spot 12–48; GameSimulator runs it after any score, replacing flat touchback; return TDs deferred); **punt returns + muffs** (`Punt(rng, offense, returnTeam)` now nets gross−return, rare muff = `PlayOutcome.Muffed` → `DriveResult.OffenseRetains` so the kicking team recovers and possession does NOT flip). XP/PAT already existed. New `tests/TrainingAndSpecialTeamsTests.cs` (6): AI-boost determinism, user-plan drills/rest math, **offense boost demonstrably lifts scoring** (40-seed aggregate), kickoff spot in-bounds, punt net in range, **2-pt attempts occur** (log scan over ≤250 games). **76 tests green**, full build clean, demo game sane. Compile+demo-verified; UI not eye-verified.

**Special-teams pass 2 (return TDs + blocked kicks):** `PlayOutcome` gained `Blocked` + `ReturnTouchdown`; `DriveResult` gained `DefenseReturnTd`. `SpecialTeamsResolver`: `KickoffReturnSpot(int)` REPLACED by `Kickoff(rng, receivingTeam) → KickoffResult(Spot, ReturnTouchdown)` (record struct); kickoff/punt return TDs are **gated on returner Speed ≥ `FastReturner`=15** then a sub-1% roll scaling with speed (`<= 4 + (speed-15)*2` per /1000 kickoff; punts `<= 3 + (speed-15)*2`, only on a true return roll>65) — "fairly rare, fast players only." Blocks: FG ~3%, punt ~2%, XP ~2% (rare `rng<=N`), set `Blocked`+KickGood=false. Blocked punt = negative net → existing drive math hands the return team a short field; blocked FG → existing missed-FG path (defense ball), logged "BLOCKED". `DriveSimulator` punt block: muff→OffenseRetains, ReturnTouchdown→`DefenseReturnTd` (defense scores), else normal/blocked net. `GameSimulator`: `DefenseReturnTd` awards the defense **7** then the offense receives the ensuing kickoff (no flip, touchback-25); post-score kickoffs now call `Kickoff` and on `ReturnTouchdown` award the receiving team 7, then flip back so the original scorer receives (no TD-chaining — ensuing kickoffs are plain 25). Return TDs auto-grant 7 (XP assumed, no separate roll). Tests updated (`Kickoff` not `KickoffReturnSpot`; new `ReturnTouchdowns_HappenButAreRare` asserts >0 and <5% of kickoffs; punt-net test skips Blocked/ReturnTouchdown). **77 tests green.** Demo sweep: ~8% of UGA-vs-ALA games show a return TD/block (rarer for non-elite). Deferred: kickoff-return-TD ensuing return chains, blocked-kick return TDs, separate return stats in the box score.

**v1.1 specced (design-first, not yet built):** `docs/v1_1.qmd` is the full v1.1 design — **SimCast** (a *simulation broadcast* of the user's game; NOT 3D), a pre-game **Gameplan** the AI caller honors (personnel/usage/carry-share/target-share/reads/QB archetype/tempo/aggression), a real **playbook of formations + concepts** with **offensive & defensive schemes** (Air Raid/Spread-RPO/Pro-Style/Smashmouth/Option vs 4-3/3-4/nickel/man-press/zone) and **Coach attributes** wired into calling/clock/advantage, all behind a realized **`IPlayCaller`** seam. Foundation = a typed **`GameTimeline`** (Snap/Pause/Score/PossessionChange segments) the broadcast plays back at a chosen **speed** (pure presentation; string log becomes a derived view). Plus a real **clock/pacing model** (tempo runoff, clock stops, timeouts, TV timeouts, 2-minute) = the "pauses in the record." **Play Match** dropdown on game day = SimCast / Quick Sim / Sim-to-Halftime / **Jump-In (disabled)** — Jump-In is the seam to a FUTURE 3D live-play-calling mode (ships the interface + grayed button now, not the mode). Determinism: gameplan/scheme/coach are inputs; SimCast speed never touches RNG. **User decisions locked:** design-doc-first → then **P1 = SimCast + pacing**; control model = pre-game gameplan + watch (Jump-In later); scheme depth = **richer playbook/concepts** (not abstract buckets). Phasing P1 (SimCast+timeline+clock) → P2 (gameplan+usage+IPlayCaller+QB archetype/reads) → P3 (schemes+coach wiring, AI gameplans, league-wide identities) → P4 (drive chart, narrative beats, balance). Decisions.qmd open Qs #2 (play catalog) + #3 (clock fidelity) marked resolved-in-v1.1. **v1.1 doc corrections (round 2):** **Play Game** dropdown (renamed from Play Match) = **Playcaller** (disabled, future 3D live-calling) / **SimCast** / **Quick Sim**. **Jump-In is NOT a Play Game option** — it's a control INSIDE SimCast = "jump into an ongoing match" (mid-game handoff to Playcaller); also disabled now. SimCast is a **tabbed game hub** (modeled on ESPN gamecast): **Gamecast** (live PBP + light commentary) / **Play-by-Play** (drives are first-class objects = collapsible cards "TD · 7 plays, 83 yds, 4:05" → plays) / **Box Score** / **Team Stats** / **Game Info**. Timeline restructured to **Game→Drive→Segment** (drives first-class). **Clock = real NCAA rules** (researched): 1st-down-in-bounds KEEPS clock running EXCEPT last 2 min (2023 change); **2-min timeout** in Q2/Q4 (2024 new); incomplete/OOB/change-of-possession/score stop clock; tempo = pre-snap burn. NEW v1.1 scope added: **Stadiums** (`Stadium`: name/city/capacity; data-fill ~130 in FbsLeagueData) + **Attendance simulation** (drivers: capacity/prestige/record/opponent/rivalry/slot → attendance + %capacity) → **home-field advantage** (engine has NO HFA today; crowd% tilts the dice via advantage system, road offense Composure/3rd-down/red-zone/4Q; deterministic input) → first driver of the future **ticket→NIL economy** (v2 flow, v1.1 input). P1 now = SimCast + college clock + stadiums/attendance/HFA + the tabbed hub. **Media bug FIXED:** `MediaPage` listed weeks via `WeeksWithCoverage(Year)` (year-filtered) while the Headlines card reads all articles → cross-season/year-stamp mismatch could hide coverage; now lists all `(Year,Week)` from `Media.Articles` (year-robust, labels off-year weeks). Couldn't repro single-season headlessly (can't run editor); if it persists in season 1 it's an ItemList render issue. **v1.1 P1 STARTED — GameTimeline refactor done (the load-bearing foundation):** new `src/CfbSim.Core/Sim/Game/Timeline.cs` — `GameTimeline` (List<`Drive`>), `Drive` (OffenseId/Abbr, StartBallOn, StartClockLabel, PrecedingMarker for "== HALFTIME ==/OVERTIME N ==", Result, Points, Segments; computed `Header`/`PlayCount`/`Yards`), `GameSegment` (Kind=Snap/SpecialTeams/Score, ClockLabel/Elapsed, Text, + structured Down/Distance/BallOn/PlayType/Yards/Touchdown/FirstDown/Turnover, + Score fields ScoreKind/HomeScore/AwayScore). **The string play log is now DERIVED** (`GameTimeline.PlayLog()` = markers+headers+non-score segment Text; `ScoringSummary()` = Score segments' Text). `GameResult` holds `Timeline` (in `CfbSim.Core.Sim.Game`; Model→Sim namespace ref, same assembly, fine) and `PlayLog`/`ScoringSummary` are get-only derived props (no more `.Add`). `DriveSimulator.Run` signature changed `List<string> log`→`Drive drive`; every `log.Add(text)` became `Add(drive, clock, kind, text, ...structured...)` via a private helper — **exact same strings preserved** (2-pt/return-TD/etc. log scans still pass). `GameSimulator` builds drives via `StartDrive(timeline, offense, ballOn, clock, ref pendingMarker)`, sets `drive.Result/Points`, and `AddScore(drive, ...)` now appends a Score segment instead of a separate list; halftime/OT use `pendingMarker`. Determinism preserved (no RNG changes — structural only). OT scores still NOT recorded as Score segments (pre-existing; OT not in ScoringSummary). Kickoff/punt return-TD Score segments attach to the triggering drive. New `tests/TimelineTests.cs` (4): drives exist + log derives + scoring excluded from log; scoring summary tracks final; drive play/yard counts sane; deterministic. **81 tests green**, full build clean, demo sane. Next P1 steps: college clock model (tempo/stops/2-min/timeouts) → stadiums+attendance+HFA → the SimCast tabbed-hub UI (Gamecast/Play-by-Play drive cards/Box/Team Stats/Game Info) reading the timeline. **Next action: continue P1.** Note: current `PlayCaller` is a STATIC class w/ one `passBias` knob (not yet an interface); `GameClock` is elapsed-seconds only (no stoppages); `Coach` attrs exist but are UNUSED; schemes not modeled; resolvers pick RB/WR via uniform `Pick(pos,rng)` — all of that is what P2/P3 overhaul.

**Godot UI gotcha (recurring):** a code-built control only fills space if it has `SizeFlags.ExpandFill` AND every ancestor up the chain also does. Symptom of a miss: cards/panels render their title but the body collapses to zero height (looks "empty/broken"). `NavHost.Build` sets ExpandFill on its root for this reason; any new container that should fill must too.

Next: M9 = Balance & Polish (tune curves/distributions; smarter poll; parity). UX follow-ups: university view (we don't model universities separately yet), persist box scores (session-only now), richer player bio/career data, scroll-position preservation, background sim threading.
